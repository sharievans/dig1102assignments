var util = require ('util');// Variables, basic expressions, and comments:var laura = 'brillant!'; // Uh, not really. :/// Using some basic assignment and arithmetic operators:    var counter = 1; counter = 2;    counter = 1 + 1;    counter = counter + 1;    counter += 2;        // Getting some feedback:util.print(counter); // Actually harder than that... console.log(counter); // Always available (almost).